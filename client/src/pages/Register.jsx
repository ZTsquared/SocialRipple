import React from 'react'

export default function Register() {
  return (

    // the form to add a new user. Should take basic info in different fields, but specially
    // if the user is an individual user or an organisation and the default interests to set the filters
    // when you start the app.
    
    <div>Register</div>
  )
}
